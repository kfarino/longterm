# Sibling / new-household setup kit

*Approved 2026-08-04 — Kevin. For sharing Longterm with a sibling (full stack: dashboard + Monarch + Telegram + Calendar).*

## Deliverables

- `examples/*.example.json` — valid schemas, fictional “Alex & Jordan” household; owner keys stay `kevin`/`hanna` (code-fixed slots) with display names from the interview.
- `docs/setup-with-ai.md` — LLM playbook (one question at a time; write files; run builds; wire automation).
- `SETUP.md` — short human entry: clone, open AI, paste one prompt.
- README link to SETUP.

## Non-goals

- Do not commit real financial data.
- Do not rename `kevin`/`hanna` balance keys in this pass (document as Person A / Person B slots).
